import React from 'react';

const Movies = () => (
  <div style={{ padding: '1rem' }}>
    <h1>Movies Page</h1>
    <p>This is a placeholder. You can add filtering, search, etc. here later.</p>
  </div>
);

export default Movies;
